#Discovery server hostname

#Server configuration
server.port = ${port:8030}
ssl.port = 9032
stb.notification.Protocol = TCP
tqs.server.accessUrl= http://10.125.133.85:8082//triggerservice/writeTrigger
tqs.server.maxRetries=2
background.process.repeatInterval=3
spring.application.name = UNICASTSERVICE

#spring.profiles.active = ${profile:PROD}

spring.jmx.default-domain = ${spring.application.name}

spring.messages.basename = classpath:messages/message

spring.cloud.config.enabled = false

ribbon.eureka.enabled = false
eureka.client.enabled=false

endpoints.restart.enabled = true

endpoints.refresh.enabled = true

